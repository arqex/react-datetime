(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(['react'], factory);
    } else {
        // Browser globals
        root.ReactBootstrapDateTimePicker = factory(root.React);
    }
}(this, function (React) {

var process = {env:{NODE_ENV:'production'}};
